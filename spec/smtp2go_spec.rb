require "spec_helper"

RSpec.describe Smtp2go do
  it "has a version number" do
    expect(Smtp2go::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
